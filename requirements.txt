# -- UI --------------------------------------------------------------
streamlit>=1.35.0

# -- PDF parsing -------------------------------------------------------
pymupdf>=1.24.0

# -- OCR -----------------------------------------------------------------
pytesseract>=0.3.10

# -- Audio transcription -------------------------------------------------
faster-whisper>=1.0.0

# -- Image handling --------------------------------------------------------
Pillow>=10.0.0
imagehash>=4.3.1

# -- Embeddings ------------------------------------------------------------
sentence-transformers>=3.0.0
open-clip-torch>=2.24.0
torch>=2.0.0

# -- Chunking / tokenisation -----------------------------------------------
tiktoken>=0.7.0

# -- File type detection ----------------------------------------------------
filetype>=1.2.0

# -- Vector DB -------------------------------------------------------------
chromadb>=0.5.0

# -- Config / env ------------------------------------------------------------
python-dotenv>=1.0.0

# -- HTTP client ---------------------------------------------------------
httpx>=0.27.0

# -- Groq LLM client ----------------------------------------------------
groq>=0.9.0

# -- Dev / test only ---------------------------------------------------
pytest>=8.0.0

filetype>=1.2.0